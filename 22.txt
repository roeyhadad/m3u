Private Sub Application_NewMailEx(ByVal EntryIDCollection As String)
    Dim arrIDs() As String
    Dim i As Long
    Dim itm As Object
    Dim mail As Outlook.MailItem

    arrIDs = Split(EntryIDCollection, ",")

    For i = LBound(arrIDs) To UBound(arrIDs)
        Set itm = Application.Session.GetItemFromID(arrIDs(i))
        If TypeOf itm Is Outlook.MailItem Then
            Set mail = itm

            ' נבדוק רק על המיילים שמעניינים אותנו
            If InStr(mail.Body, "לאישור הבקשה") > 0 And _
               InStr(mail.Body, "גיל בדוסה") > 0 Then

                MsgBox "זוהה מייל מתאים: " & mail.Subject, vbInformation, "Match"

                g_AutoApprovalInProgress = True
                mail.Display
                ClickApprovalLink mail
            Else
                ' דיבוג – לראות למה לא נכנס:
                'MsgBox "מייל לא מתאים: " & mail.Subject, vbInformation, "No Match"
            End If
        End If
    Next i
End Sub
